# Copy to .env and fill in. Never commit .env.
# LLM (optional — app works offline with built-in Mock provider)
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
PRIMARY_MODEL=mock
# Database
SIGNALCRAFT_DB_PATH=data/signalcraft.db
# App
SIGNALCRAFT_ENV=dev
